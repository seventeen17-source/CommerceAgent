# 2026-09-18

## 今日目标

- T009：实现 User / Order / OrderItem / Shipment / LogisticsEvent 的 JPA Entity 与 Repository，保留 immutable ownership、乐观锁 `version`、权威价格/状态
- 按 AGENTS.md 新规则，把 `@Version` 与"防重复"的区别作为 **interview checkpoint** 讲透，并用可执行测试证明

## 今日完成

- [x] 新增 `user/` 包：`User`、`UserRole`、`UserStatus`、`UserRepository`（`findByUsername`）
- [x] 新增 `order/` 包：`Order`、`OrderItem`、`OrderStatus`、`AfterSalesStatus`、`OrderRepository`、`OrderItemRepository`
- [x] 新增 `logistics/` 包：`Shipment`、`ShipmentStatus`、`LogisticsEvent`、`ShipmentRepository`、`LogisticsEventRepository`
- [x] `OrderRepository` 新增的 Customer-facing 查询均带 ownership 维度：`findByOwnerIdOrderByCreatedAtDesc`、`findByIdAndOwnerId`、`existsByIdAndOwnerId`；但继承自 `JpaRepository` 的 `findById` / `findAll` 等仍然存在，真正的 Customer API 授权边界必须在 T011/T019 的 service 层强制
- [x] 新增 `OrderConcurrencyGuaranteesTest`（3 个测试），**用可执行证据证明**：① Order 过期快照写入被乐观锁拒绝；② Shipment 过期快照写入同样被乐观锁拒绝；③ 同一订单的第二条运单被唯一约束拒绝（与 `@Version` 无关）
- [x] `mvnw verify` → **BUILD SUCCESS**：`Tests run: 7, Failures: 0, Errors: 0`（CommerceBackendApplicationTests 1 + CoreSchemaMigrationTests 3 + OrderConcurrencyGuaranteesTest 3），Spotless 21 文件 clean，SpotBugs 通过，JaCoCo 报告产出

## 遇到的问题

- **`ddl-auto: validate` 第一次就抓出真实的类型不匹配（这正是它的价值）**：实体按默认把 `String currency` 映射成 `varchar(3)`，而 V001 建的是 `CHAR(3)`。启动直接失败：
  `Schema validation: wrong column type encountered in column [currency] in table [orders]; found [bpchar (Types#CHAR)], but expecting [varchar(3) (Types#VARCHAR)]`。
  修法是**让实体如实声明数据库的类型**（`@JdbcTypeCode(SqlTypes.CHAR)`），**绝不能反过来改已执行过的 V001** —— 那会让 Flyway 的 checksum 校验失败，`spring-boot:run` 直接起不来。
- **同一类问题的第二处**：`logistics_events.description` 在 V001 里是 `TEXT`，Hibernate 默认按 `varchar(255)` 期望，同样会被 validate 拦下。用 `@JdbcTypeCode(SqlTypes.LONGVARCHAR)`（PostgreSQL 方言下即 `text`）声明真实类型。
- **SpotBugs 抓到两条真实告警，而且逼出了更好的设计**：`OrderItem.getOrder()` 与 `LogisticsEvent.getShipment()` 报 `EI_EXPOSE_REP`（返回内部可变引用）。它的实际危害是：调用方可以 `item.getOrder().setStatus(...)` **绕过订单聚合根**改状态。修法不是加 `@SuppressFBWarnings` 压制，而是**只暴露 id**（`getOrderId()` / `getShipmentId()`）—— 顺带把聚合边界画得更干净。
- **ASCII 直引号混进 Java 字符串导致编译失败**：断言消息里写了 `（这正是"丢失更新"要防的事）`，其中引号是 ASCII 而不是中文弯引号，字符串被提前截断，`mvnw` 报 `需要')'或','`。改成不含内层引号的写法。**教训：中文文案里夹引号时，要么用「」，要么确认是弯引号。**
- **PowerShell `Select-Object -First N` 会掐断上游管道**（本会话已第二次踩）：用它过滤 `mvnw verify` 输出会让 Maven 中途被杀、`exit=-1`，看起来像构建失败。正确做法是先落盘再筛选。

## 关键决策

- **`User` 单独放 `user/` 包**：`order/`、`logistics/` 都装不下它；plan.md 的模块列表不是穷举。这是对 plan.md 的一个小增补。
- **状态字段用 Java 枚举收紧，但明确它不是数据库完整性约束**：`orders.status` / `users.role` / `users.status` 库里有 CHECK 且文档定义了取值 → 枚举；`shipments.status` / `orders.after_sales_status` 库里**没有 CHECK** → 也用枚举，但实体与枚举的文档里都写明：**枚举是代码里的规则，只管得住走这段代码的路径；手工 SQL、另一套服务、批量导入照样能写入任意字符串。** 要真正强制必须后续 Flyway 迁移补 CHECK。
- **枚举取值为 V1 最小集，且刻意不发明领域值**：`ShipmentStatus` 只定义 `CREATED / IN_TRANSIT / DELIVERED`（够 US1/US2 用），`AfterSalesStatus` 只定义 `REFUND_REQUESTED / RETURN_REQUESTED`；完整状态机由 T024/T026 定义后扩展。`after_sales_status` 保持可空，`null` 即"尚无售后"，不造 `NONE` 占位值。
- **乐观锁测试属于 T009，不推迟到 T019**：本任务引入了 `@Version`，那么"它到底有没有生效"就必须在本任务内有证据。把它挪到后面，等于让一个未验证的机制先进入代码库。Review 后又为 `Shipment.version` 补了同类 stale-update 测试，避免只验证 Order 而把 Shipment 的锁当作默认正确。
- **Review 后简化 `Shipment.order_id` 映射**：不再为 `orders` 表创建第二个 `OrderRef` 实体，而是把 `order_id` 直接映射成不可变 `String orderId`。订单存在性继续由数据库 FOREIGN KEY 保证，V1 的 0/1 Shipment 基数继续由 UNIQUE 保证；这样既避免 `logistics -> order` 包依赖，也避免同一张 `orders` 表被两个 Entity 重复映射。
- **`Order` 上不放物流的反向引用**：聚合边界画在订单+明细之内；物流按需经 `ShipmentRepository.findByOrderId` 查。
- **`logistics_events` 不挂集合、`order_items` 挂集合**：明细属于订单聚合（建订单一起写、删订单一起删，V001 是 `ON DELETE CASCADE`），事件是追加式事实记录、按时间查最近一条，生命周期独立。
- **`OrderItem` 当前按创建后不可变处理**：`Order.@Version` 不会自动保护对子表 `order_items` 的直接 UPDATE。V1 不提供 quantity/unit_price 等 setter；如果未来允许修改明细，必须显式补明细版本或设计能可靠推进订单版本的并发控制，不能假设聚合根 version 会自动覆盖子表写冲突。

## 今天学到

- **数据库约束的定位：它是"数据本身的规则"，不是"代码里的检查"**。代码里的 `if (exists) throw` 只管得住走这段代码的路径；DBA 手工 SQL、另一套服务、批量导入、并发线程都能绕过去。约束谁都绕不过。
- **四类约束一句话记住**：`NOT NULL` 不能空、`UNIQUE` 不能重复、`FOREIGN KEY` 不能指向不存在的东西、`CHECK` 只能是这几样之一 —— 本项目 V001 里四类全都用上了。
- **PRIMARY KEY = UNIQUE + NOT NULL，且每表只能有一个**。所以"主键能防重复"是对的，但它只防**自己那一列**：
  - 实测：`demo_pk` 表里 `id` 是主键、`order_id` 是 UNIQUE。插 `('S-9','O-1')`（新 id、重复 order_id）时，**主键完全满意，是 UNIQUE 把它拦下的**。若只有主键没有 UNIQUE，两条不同 id 就能指向同一订单 —— 于是"改哪个、删哪个、查哪个"全都没有答案。
  - 两者的实际差异：主键**不允许 NULL**（实测报 not-null violation），UNIQUE **允许 NULL 且多个 NULL 共存**（实测两行 NULL 都插入成功）。后者意味着：**幂等键若可空，唯一约束形同虚设**。
- **"先查再插"在并发下不够（TOCTOU）**：`SELECT ... 不存在` 与 `INSERT` 之间有时差，两个事务都能通过检查然后都插入。这个洞**只能由能仲裁并发的组件（数据库）来堵**。
- **两种失败必须分清，对策不同**：丢失更新（同一行被覆盖）→ 乐观锁 `@Version`；重复创建（插进两行）→ 唯一约束。**"改了"和"插了"是两件事。**
- **`ddl-auto: validate` 是一个免费的映射预言机**：它把"实体与数据库不一致"在**启动时**暴露，而不是等到运行时 500。今天它一次抓出 `CHAR(3)`、一次抓出 `TEXT`。
- **静态分析工具的正确用法是改设计，不是加注解压制**：`EI_EXPOSE_REP` 指向的是"聚合根引用被泄漏"，压制它等于把问题留在代码里。

### T009 面试知识点（检查点结论）

> **`@Version` 乐观锁防的是「丢失更新」，不防「重复插入」。**
>
> 原理：乐观锁靠"读到的版本号"作为暗号，UPDATE 时带 `AND version = ?`，影响 0 行就说明有人先改过。而并发**插入**的两个事务各自插入新行，版本号都是初始值，**没有共同的暗号可比**。
>
> 所以"同一笔退款只能有一条"必须靠**数据库唯一约束**（建在业务幂等键上）。重复请求到达时，捕获唯一约束冲突 → **回查已有记录 → 按成功返回**，而不是报错：重复请求不是错误，是同一逻辑操作的重放；返回错误会让调用方误判成功与否，在资金场景里更危险。
>
> 一句话分工：**乐观锁管"更新冲突"，唯一约束管"重复创建"，幂等处理负责"冲突时返回什么"。**
>
> 常见追问：乐观锁冲突后能不能重试？（写操作不能盲目重试，必须先查权威状态）`REPEATABLE READ` 能替代它吗？（不能，隔离级别不阻止丢失更新）唯一约束冲突怎么翻译成业务响应？（这就是幂等实现的核心）

## T009 post-review hardening（已完成）

- `Order.createdAt` / `User.createdAt` 增加 Hibernate `@Generated(event = EventType.INSERT)`：继续由 PostgreSQL `DEFAULT CURRENT_TIMESTAMP` 生成，但 INSERT 后由 Hibernate 把数据库值同步回实体。
- 新增 `DatabaseGeneratedValuesTest`：验证 `User.saveAndFlush(...)` 返回实体的 `createdAt` 非空，以及 `Order.saveAndFlush(...)` 后当前 Order 实例的 `createdAt` 非空。
- `duplicateShipmentForSameOrderIsRejectedByUniqueConstraint` 改用 `saveAndFlush` 强制 SQL 当场执行，并从异常链提取 Hibernate `ConstraintViolationException`，断言 PostgreSQL SQLState 为 `23505` 且 `constraintName == shipments_order_id_key`。
- 本地最终复验：`mvnw.cmd verify` → **Tests run: 8, Failures: 0, Errors: 0 / BUILD SUCCESS**。`DatabaseGeneratedValuesTest` 1/1 通过；`OrderConcurrencyGuaranteesTest` 3/3 通过；日志确认 PostgreSQL `SQLState 23505` 且命中 `shipments_order_id_key`。

## T010 — AfterSalesRule persistence（已完成）

- 新建独立分支：`foundation/after-sales-rule`，基于 T009 最终文档 checkpoint `ae469e9`。
- 新增 `eligibility/AfterSalesRule.java`：完整映射 V001 的 `commerce.after_sales_rules` 字段。
- 新增 `AllowedAction`：`REFUND_ONLY / RETURN / RETURN_REFUND / MANUAL_REVIEW / DENY`，与数据库 CHECK 对齐。
- `AfterSalesRule.version` 明确是**业务规则版本号**，不是并发控制字段，因此刻意不使用 JPA `@Version`。
- 规则主体字段按版本化快照处理为不可更新映射；`active` 保留为独立状态字段。T010 不提前实现 T025 的规则匹配/eligibility 算法。
- 新增 `AfterSalesRuleRepository`，仅提供 `findByRuleCodeAndVersion` / `existsByRuleCodeAndVersion` 等持久化级查询，不把业务规则选择逻辑塞进 Repository。
- 新增 `AfterSalesRulePersistenceTest`：
  1. 全字段 round-trip；
  2. 重复 `(rule_code, version)` 时断言 PostgreSQL SQLState `23505`；
  3. 进一步断言具体命中 `uq_after_sales_rules_code_version`，避免因其他完整性错误产生 false positive。
- 实现提交：`7e98ff0 feat(t010): add after-sales rule persistence`。
- **最终验收**：用户本地 `mvnw.cmd verify` → **BUILD SUCCESS**；`Tests run: 10, Failures: 0, Errors: 0, Skipped: 0`。其中 `AfterSalesRulePersistenceTest` 的 2 个测试已纳入总数并通过。

## T011 — JWT security（已完成）

- 新建独立分支：`foundation/jwt-security`，基于 T010 最终 checkpoint `f4d9355`。
- 新增 Bearer JWT Resource Server 支持；HTTP 请求默认需要认证。
- 新增 `CommercePrincipal`，业务层只暴露 `userId + role`；后续 ownership 必须从 principal 推导。
- JWT 只把验签后的 `sub` 当身份索引；当前 `role/status` 每次从 `commerce.users` 权威读取，token 中额外的 `role` claim 不作为授权来源。
- 新增本地 HS256 JWT 配置与 dev/test/eval fixture issuer；完整 OAuth/OIDC 不属于 V1。
- SecurityFilterChain 采用 STATELESS session；health/info 匿名，其余请求默认认证。
- 新增集成测试覆盖：合法 JWT、缺 token、签名篡改、过期 token、伪造 role claim 不覆盖 DB 角色、DISABLED 用户即使 token 合法也拒绝。
- 主要实现 checkpoint：`01e5427`；后续配置提交：`13e83ac` / `edee13b` / `daab2d3`。
- **尚未验收**：需要本地重新执行 `mvnw.cmd verify`；通过后再勾选 T011。

## T011 — Local JWT security（实现完成，待验收）

- 新建独立分支：`foundation/jwt-security`，基于 T010 最终 checkpoint `f4d9355`。
- 引入 Spring Security OAuth2 Resource Server，使用 Bearer JWT，不实现完整 OAuth/OIDC 登录产品。
- 新增 `security/JwtProperties`、`SecurityConfig`、`CommercePrincipal`、`CommerceJwtAuthenticationConverter`、`CommerceJwtAuthenticationToken`、`LocalJwtIssuer`。
- JWT 使用本地 HS256 对称密钥验证签名，同时校验 issuer 与 exp；服务端保持 stateless，不建立 token/session 表。
- 关键边界：JWT 的 `sub` 只负责证明“是谁”；当前用户 `status` 与 `role` 每次认证时从权威 `commerce.users` 重新读取，因此 token 中伪造/过期的 role claim 不能覆盖数据库角色。
- `CommercePrincipal(userId, role)` 作为业务层统一身份对象；后续 Customer ownership 必须从 principal 推导，而不是接受任意 userId 参数。
- `/actuator/health`、`/actuator/info` 允许匿名，其余 HTTP 默认要求认证。
- 新增 `JwtSecurityIntegrationTest`，覆盖：合法 JWT、缺少 JWT、签名篡改、过期 JWT、role claim 不可覆盖 DB role、DISABLED 用户即使 token 签名有效也必须拒绝。
- 主要提交：`b19b436 feat(t011): add jwt authentication and role-aware principal`。
- **尚未验收**：需本地先执行 `mvnw.cmd spotless:apply`，再执行 `mvnw.cmd verify`；通过后才勾选 T011。

## 下一步

- 验收 T011：本地 JWT 认证和 role-aware principal；通过后进入 T012
- 未决：`.specify/feature.json` 本地仍指向 `specs/001-agent-career-project`

## Git 证据

- Branch: `foundation/core-schema`
- T009 初始 checkpoint：`5ece3d1`；review 后结构收紧 checkpoint：`e03f415`；post-review hardening：`1886478`；Spotless 收尾：`008af1f`
- Tasks: T008–T010 完成；当前进入 T011
- 最终本地验证: `mvnw verify` → `Tests run: 8, Failures: 0, Errors: 0` / **BUILD SUCCESS**；完整 verify 通过；`target/site/jacoco/index.html`
- 关键测试: `OrderConcurrencyGuaranteesTest.staleUpdateIsRejectedByOptimisticLocking`、`staleShipmentUpdateIsRejectedByOptimisticLocking`、`duplicateShipmentForSameOrderIsRejectedByUniqueConstraint`

### T010 Git / 验收补记

- Branch: `foundation/after-sales-rule`
- Implementation: `7e98ff0 feat(t010): add after-sales rule persistence`
- Spotless fix: `3fd2177 style(t010): apply spotless formatting`
- Local acceptance: `mvnw.cmd verify` → **Tests run: 10, Failures: 0, Errors: 0, Skipped: 0 / BUILD SUCCESS**

### T011 Git / 验收补记

- Branch: `foundation/jwt-security`
- Core implementation: `f8361de feat(t011): add JWT authentication and authoritative principal`
- Spotless formatting: `a8fcf5d style(t011): apply spotless formatting`
- SpotBugs fix: `1e85c5b fix(t011): make security principal serializable`
- Local acceptance: `mvnw.cmd verify` → **BUILD SUCCESS**
- T011 状态：完成；下一任务：T012 统一 error envelope。


## T012 — Unified Error Envelope（已完成）

- 新建独立分支：`foundation/error-envelope`，基于 T011 最终 checkpoint `fbf7d27`。
- 新增 `common/error/ErrorCode`、`ErrorResponse`、`BusinessException`、`GlobalExceptionHandler`。
- Spring MVC 业务异常通过 `@RestControllerAdvice + @ExceptionHandler` 统一转换为稳定 JSON error envelope。
- Spring Security Filter 链中的 401/403 分别通过 `RestAuthenticationEntryPoint` / `RestAccessDeniedHandler` 返回同样结构，避免安全错误绕过 MVC error contract。
- 新增 `TraceIdFilter`：每个 HTTP 请求生成服务端 traceId，写入 `X-Trace-Id`、MDC 和错误响应体。
- 新增通用 `ACCESS_DENIED`，与订单 ownership 专用 `ORDER_FORBIDDEN` 分离，避免把角色权限不足误报成订单越权。
- `INTERNAL_ERROR` 不向客户端泄露原始异常消息；详细异常只进入服务端日志，并用 traceId 关联。
- 新增 `ErrorEnvelopeIntegrationTest`，覆盖：无认证、篡改 JWT、业务异常、Bean Validation、内部异常不泄露、403 handler。
- 核心实现提交：`729a591 feat(t012): add unified error envelope`。
- **最终验收**：用户本地 `mvnw.cmd verify` → **Tests run: 22, Failures: 0, Errors: 0, Skipped: 0 / BUILD SUCCESS**；Spotless 与 SpotBugs 均通过。


### T012 验证过程补记

- 第一次本地 `mvnw.cmd verify`：**Tests run: 22, Failures: 0, Errors: 0, Skipped: 0**，说明编译与全部测试通过；随后 Spotless 报 5 个纯格式差异，未达到任务验收标准。
- 用户本地执行格式修复后再次验证：Spotless 报 **0 needs changes**，格式门禁通过。
- 随后 SpotBugs 报 3 个 `EI_EXPOSE_REP2`：
  - `RestAccessDeniedHandler.responseWriter`
  - `RestAuthenticationEntryPoint.responseWriter`
  - `SecurityErrorResponseWriter.jsonMapper`
- 这 3 个字段均为 Spring singleton 的 constructor-injected private final collaborator，没有 getter，也没有把引用暴露给调用方；与 T009 中真实的领域对象引用泄漏不同，判定为框架 DI 场景下的保守误报。
- 处理方式不是全局关闭 `EI_EXPOSE_REP2`，而是在 `commerce-backend/config/spotbugs-exclude.xml` 中只排除上述 3 个精确 class/field/pattern；其余代码仍继续受该规则检查。
- SpotBugs 窄范围排除提交：`77b5618 fix(t012): narrow SpotBugs exclusions for Spring DI`。
- 当前状态：**T012 已正式完成**；最终本地 `mvnw.cmd verify` → **BUILD SUCCESS**。


### T012 Git / 验收补记

- Branch: `foundation/error-envelope`
- Core implementation: `729a591 feat(t012): add unified error envelope`
- SpotBugs narrow exclusion: `77b5618 fix(t012): narrow SpotBugs exclusions for Spring DI`
- Java quality-gate rules persisted: `b79be65 docs: persist Java quality gate workflow`
- Local acceptance: `mvnw.cmd verify` → **Tests run: 22, Failures: 0, Errors: 0, Skipped: 0 / BUILD SUCCESS**
- T012 状态：完成；下一任务：T013 结构化业务/安全 Audit Writer。


## T013 — Structured Audit Writer（已完成）

- 新建独立分支：`foundation/audit-writer`，基于 T012 最终 checkpoint `579f108`。
- 新增 `audit/AuditActorType`、`AuditEvent`、`AuditLog`、`AuditLogRepository`、`AuditWriter`。
- `AuditLog` 映射现有 `commerce.audit_logs`，保持 append-only：所有业务字段均不可更新；`created_at` 继续由数据库生成。
- `AuditWriter.write(...)` 使用默认 `@Transactional` / REQUIRED 语义：如果在已有业务事务中调用，则 audit 与业务写入一起提交或回滚，避免业务回滚后留下“SUCCESS”的假审计证据。
- 新增 `AuditMetadataPolicy`：递归拒绝 raw auth token、JWT-like 值、password/secret/apiKey/cookie、hidden chain-of-thought/internal reasoning/raw prompt 等敏感 metadata；允许结构化 `reasonCodes`、`traceId`、eligibility 结果等明确业务事实。
- `AuditEvent` 创建时对 metadata 做 snapshot，避免调用方后续修改原 Map 导致审计内容漂移。
- 新增 `AuditWriterIntegrationTest`，覆盖：
  1. 结构化 audit event round-trip；
  2. raw credential / hidden reasoning 递归拒绝；
  3. metadata snapshot；
  4. audit 加入业务事务并随 rollback 消失；
  5. 允许结构化 reason codes。
- 对 `AuditWriter.repository` 的 Spring constructor-injected private final collaborator 预先使用与 T012 相同的精确 SpotBugs `EI_EXPOSE_REP2` 排除；其余代码继续受该规则检查。
- 核心实现提交：`81c513e feat(t013): add structured audit writer`。
- **最终验收**：用户本地 `mvnw.cmd verify` → **BUILD SUCCESS**；测试、Spotless、SpotBugs 均通过。


### T013 hardening

- 针对 review 中的三个非 bug 改进点，决定全部在 T013 内收紧，而不是留成模糊待办。
- **敏感串检测加强**：JWT 检测由整串匹配改为任意位置 `find()`；Bearer token 也改为任意位置、大小写不敏感检测，避免 token 嵌入普通 note 字段时漏过。
- **顶层字段同样受敏感信息策略保护**：`actorId`、`action`、`resourceType`、`resourceId`、`result` 在长度/非空校验之外，也统一经过 token 检测，避免绕过 metadata policy。
- **事务边界正式拆分**：
  - `writeBusinessEvent(...)`：默认 `REQUIRED`。关键业务写入与成功审计同事务提交/回滚；若 audit 写失败，则关键业务也不应被视为完成。
  - `writeSecurityEvent(...)`：`REQUIRES_NEW`。越权拒绝、认证/授权失败等已经确定发生的安全事件使用独立事务，即使外层业务事务回滚也保留证据。
- 新增测试证明两种语义：
  1. business audit 随外层 rollback 消失；
  2. security audit 在外层 rollback 后仍存在；
  3. embedded JWT / embedded Bearer 会被拒绝；
  4. 顶层 audit 字段塞入 token 也会被拒绝。
- Hardening 提交：`320b6d2 fix(t013): harden audit safety and transaction boundaries`。
- 当前状态：**T013 已正式完成**；最新本地 `mvnw.cmd verify` → **BUILD SUCCESS**。


### T013 Git / 验收补记

- Branch: `foundation/audit-writer`
- Core implementation: `81c513e feat(t013): add structured audit writer`
- Spotless formatting: `dcba84b style(t013): apply spotless formatting`
- Audit safety / transaction hardening: `320b6d2 fix(t013): harden audit safety and transaction boundaries`
- Final Spotless alignment: `de3e382 style(t013): align audit test with spotless`
- Local acceptance: `mvnw.cmd verify` → **BUILD SUCCESS**
- T013 状态：完成；下一任务：T014 dev/eval fixture loader。
