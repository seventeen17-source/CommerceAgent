# 2026-09-23

## T023 — customer-scoped order list/detail HTTP API

### 本次交付

- 新增 `OrderController`，暴露 `GET /api/v1/orders` 与 `GET /api/v1/orders/{orderId}`。
- Controller 不接收 `userId`；身份只从 Spring Security 注入的 `CommercePrincipal` 获取。
- `SecurityConfig` 对两个订单读端点要求 `CUSTOMER` 角色：无认证返回 401，已认证但非 CUSTOMER 返回 403 `ACCESS_DENIED`。
- ownership 继续复用 T019 `OrderService`：本人订单可读；cross-owner 与不存在对外统一为 404 `ORDER_NOT_FOUND`，避免 existence oracle。
- OpenAPI 与 Python `CommerceClient.list_orders()` 同步为 V1 无 query filter 的真实能力；`product_query/status_filter/created_after` 保留为后续 T029/T030 Tool/candidate-resolution 层能力，不冒充当前 Java `GET /orders` 已实现的参数。
- review 时发现 T022 遗留的 OpenAPI YAML 损坏：`idempotencyKey.pattern` 与 `responses` 被粘连；已恢复为合法的 `^[A-Za-z0-9_-]+$` 和独立 `responses` 节点。

### 验收证据

- T023 初次 Java 全量：`mvnw.cmd clean verify` → **BUILD SUCCESS**，**138 tests**，Failures 0 / Errors 0；Spotless、SpotBugs、JaCoCo 均通过。
- Python 全量：`pytest -q` → **261 passed, 6 skipped**；`ruff check .` → All checks passed；`ruff format --check .` → **41 files already formatted**；`mypy app` → **Success: no issues found in 25 source files**。
- Python `CommerceClient` 定点：`pytest -q tests/unit/test_commerce_client.py` → **32 passed**。
- review 后补齐 detail endpoint 的 401 / 403 覆盖；本地执行 `mvnw.cmd -Dtest=OrderHttpIntegrationTest test` → **7 tests, 0 failures, 0 errors, BUILD SUCCESS**（2026-09-23 17:52 +08:00）。
- review 后只新增测试/契约/文档；完整 Java `clean verify` 尚未在这两条新增权限测试之后再次执行，因此不把 140 tests 作为已验证事实写入记录。

### 架构位置

```text
Python Agent / CommerceClient
          ↓ HTTP
GET /api/v1/orders
GET /api/v1/orders/{orderId}
          ↓
OrderController          ← T023 HTTP boundary
          ↓
Spring Security          ← authentication / CUSTOMER capability
          ↓
OrderService             ← T019 ownership authority
          ↓
OrderRepository
          ↓
PostgreSQL
```

边界口径：
- 无/无效 JWT → 401。
- 已认证但非 CUSTOMER → 403 `ACCESS_DENIED`。
- CUSTOMER 访问自己的订单 → 200。
- CUSTOMER 访问别人的订单或不存在的订单 → 同样的 404 `ORDER_NOT_FOUND`。

### 下一步

T024：实现物流 HTTP API，复用 T019 已有 `LogisticsService` 与权威 stall calculation，并把 list/detail 同级别的 authentication / capability / ownership 测试口径继续带到物流端点。


## T024 — customer-scoped logistics HTTP API

### 本次交付
- 新增 `LogisticsController`：`GET /api/v1/orders/{orderId}/logistics`。
- HTTP 层不接收 `userId`、不自行判断“是否停滞”；身份来自 `CommercePrincipal`，ownership 与物流事实继续由 T019 `LogisticsService` / `LogisticsStallCalculator` 负责。
- 新增 `LogisticsHttpIntegrationTest`，覆盖 401、403、owner 200、cross-owner/missing 统一 404、PAID+无 shipment → 409、SHIPPED+无 shipment → 503、缺失证据保持 null、DELIVERED 后 stall 不适用。
- Flow Playground 新增 `T024 · LIVE JAVA`；Vite 增加 `/commerce/* -> Java :8080/api/v1/*` dev proxy。该步骤只验证 Web → Java，不冒充后续 T029/T032 的 Web → Agent → Tool → Java 链路。

### 验收证据
- `LogisticsHttpIntegrationTest`：**8 tests / 0 failures / 0 errors / BUILD SUCCESS**。
- Java 全量：`mvnw.cmd clean verify` → **148 tests / BUILD SUCCESS**；Spotless **90 files clean**；SpotBugs **0 warnings/errors**。
- Web：`npm.cmd run build` 成功；`npm.cmd run lint` → **0 warnings / 0 errors**。
- dev Java 成功启动在 **8080**，连接本地 `commerceagent` PostgreSQL 并加载开发 fixture。
- 5173 T024 LIVE JAVA：
  - `customer-001 / order-001` → **200**，`IN_TRANSIT`、`signed=false`、`stalledHours > 48`。
  - 同 token 访问 `order-002` → **404 ORDER_NOT_FOUND**。
  - 同 token 访问不存在订单 → **404 ORDER_NOT_FOUND**。
- 同一 Flow Playground 的 T018 live steps 本轮也重新实测：
  - **09 Create Run** → **201**，生成真实 `runId`。
  - **10 Read Run** → **200**，读回同一持久化 Run。
  - **11 Read Events** → **200**，读回至少一条 `STATE_TRANSITION / created / RUNNING` checkpoint 事件。
- 首次点击 09 出现 **502**，定位为 Agent Service `:8000` 未启动；启动 `uvicorn app.main:app --host 127.0.0.1 --port 8000` 后 09–11 全部按预期通过。该现象验证了 502 属于 Vite → Agent 上游不可达，而不是 JWT/业务拒绝。

### 架构位置
```text
T024:
Browser :5173
  ↓ Vite /commerce
Java :8080
  ↓
LogisticsController → LogisticsService → LogisticsStallCalculator → PostgreSQL

T018 live verification:
Browser :5173
  ↓ Vite /agent
Agent Service :8000
  ↓
PostgresRunStore → agent.agent_runs / checkpoints / tool trace
```

### 面试 checkpoint
`stalledHours` 是可能影响退款路径的**业务权威事实**，必须由 Java 基于数据库证据确定性计算；LLM 消费这个结构化事实做 reasoning，而不能自己从时间戳“猜”业务结论。即：**LLM 做 reasoning，Java 做 authority。**

### 下一步
T025 开始前先核对 T020 已实现的 deterministic eligibility 与原 T025 任务描述是否重叠；不重复实现已有能力，只补真实缺口并继续保留独立 checkpoint 分支。


## Git 分支策略纠正

用户再次明确长期规则：**一个功能建立一个独立分支**，而不是 T019 → T020 → T021 这样无限线性派生。

本次纠正：

- 保留 T019–T024 已有 feature 分支，作为历史 checkpoint，不删除、不重写；
- 旧 `dev/002-commerce-after-sales-mvp` 在调整前先归档，避免丢失 2026-09-15 的旧实现/文档历史；
- `dev/002-commerce-after-sales-mvp` 改为当前已验收功能的统一集成线；
- 后续每个新功能都必须从最新 dev 创建新的 `feat/...` 分支；
- 功能验收后回到 dev 集成，feature 分支继续保留用于回退；
- `main` 只在明确 Gate 完成且用户确认后更新。

目标结构：

```text
main
└─ dev/002-commerce-after-sales-mvp
   ├─ feat/功能A
   ├─ feat/功能B
   └─ feat/功能C
```

这条规则同时写入 `AGENTS.md`，作为后续所有 Agent 必须遵守的持久化协作规则。


## T025 前置核对

### 结论

T025 不需要重复实现，主体已经在 T020 为了让 deterministic eligibility 测试成立时提前落地。

现有 `EligibilityDecision` 已包含：

- `eligible`
- `allowedAction`
- `maxRefundAmount`
- `approvalRequired`
- `ruleCode`
- `ruleVersion`
- `reasonCodes`
- `evaluatedAt`

现有 `EligibilityService.evaluate()` 已完成 ownership、规则选择、按需物流取证，并把最终判定委托给纯函数式 `selectRule/decide`；`EligibilityServiceTest` 已覆盖停滞阈值、金额边界、审批阈值、缺失证据、规则冲突、重复售后、未支持动作与同输入同输出。

因此：

- T025 标记为 **已随 T020 提前交付**；
- 不创建第二个 eligibility feature 分支；
- 继续保留既有 `feat/us1-eligibility-decision` 作为这个功能的 checkpoint；
- 下一步转向 T027/T028 缺口核对，优先避免重复实现已由 T021 交付的退款核心。
